---
/**
 * FeaturedGallery
 *
 * Displays a curated selection of featured images.
 * Image data is loaded at build time via a data module,
 * then passed to a presentation-only PhotoGrid component.
 *
 * This component is fully static:
 * - No client-side fetching
 * - No hydration
 * - No runtime state
 */

import PhotoGrid from './PhotoGrid.astro';
import { featuredCollectionId, getImages } from '../data/imageStore';

// Load featured images at build time
const images = await getImages({ collection: featuredCollectionId });
---

<!-- Section heading -->
<div class="max-w-2xl mx-auto text-center mb-16">
	<h2 class="text-3xl md:text-4xl font-bold mb-4">Featured Works</h2>
	<p class="text-gray-600">
		A selection of my best photographs from various adventures
	</p>
</div>

<!-- Image grid -->
<div>
	<PhotoGrid images={images} />
</div>
